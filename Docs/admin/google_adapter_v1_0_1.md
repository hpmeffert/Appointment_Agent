# Google Adapter v1.0.1 Admin Notes

- Adds a separate `v1.0.1` Google Adapter path with booking lifecycle support.
- Supports normalized `search`, `create`, `update`, `cancel`, and `get` booking operations.
- Uses provider references separately from platform booking references.
