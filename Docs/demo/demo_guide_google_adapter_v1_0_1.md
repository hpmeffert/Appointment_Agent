# Google Adapter Demo Guide v1.0.1

## Scenario 1
Search slots for a candidate advisor and inspect normalized `provider_reference` metadata.

## Scenario 2
Create a booking, then reschedule it through the update endpoint and confirm the normalized `BookingResult`.

## Scenario 3
Cancel an existing booking and show that the platform booking reference remains stable while the provider reference is preserved.
