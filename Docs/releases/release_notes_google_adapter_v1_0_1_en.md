# Google Adapter Release Notes v1.0.1 (EN)

- Added `v1.0.1` Google Adapter as a separate versioned provider module.
- Implemented normalized search, create, update, cancel, and get booking flows.
- Added provider reference handling and lifecycle tests without breaking `v1.0.0`.
