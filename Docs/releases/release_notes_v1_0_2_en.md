# Release Notes v1.0.2 (EN)

- Refined the demo UI for live presentations with clearer controls and stronger demo storytelling.
- Carried the Orchestrator-Google integration forward into the `v1.0.2` release setup so the sales demo sits on the current internal flow line.
- Added language switching support in the demo UI for English and German.
- Added help menu links that open Demo, User, and Admin guides in separate tabs.
- Added documentation routes under `/docs/demo`, `/docs/user`, and `/docs/admin`.
- Prepared the repository for public visibility with `.env.example`, stronger `.gitignore`, and a `docker/` directory.
- Git tag and GitHub release were prepared conceptually but not created automatically because the current workspace is not an initialized Git repository.
