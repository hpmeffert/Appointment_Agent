# Release Notes v1.1.0 Patch 2

- Fixed the Google demo buttons so `Prepare`, `Generate`, `Delete`, and `Reset` execute distinct backend logic.
- Added a real preview-only `prepare` action for the Google demo calendar flow.
- Added dedicated `v1.1.0-patch2` Google adapter and demo UI routes.
- Added visible loading, success, and error feedback for Google demo actions in the UI.
- Added route and action tests for the new Google demo flow.
