# Release Notes v1.2.1

- Added `LEKAB Adapter v1.2.1` with normalized RCS/SMS send, inbound normalization, status updates, and monitor payloads.
- Added `Message Monitor` and `Communications Reports` to the Incident-style cockpit shell.
- Added shared persistent message model for provider-neutral communication tracking.
- Updated docs routes, root version, and demo base path to `v1.2.1`.
