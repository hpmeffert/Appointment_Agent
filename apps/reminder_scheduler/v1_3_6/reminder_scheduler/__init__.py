"""Reminder scheduler v1.3.6 package.

The submodules provide the API app, service layer, pure schedule calculator,
delivery dispatchers, timezone helpers, and worker entrypoints for the
hardened reminder release line.
"""
