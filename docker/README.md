# Docker Notes

This folder is reserved for future container-specific assets.

Right now the project is started with:

```bash
docker compose up --build
```

The dedicated `docker/` directory now exists so the repository structure is ready for later growth.
